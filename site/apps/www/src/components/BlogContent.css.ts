import { style } from "@vanilla-extract/css";

export const rootClass = style({
  padding: "1em"
});

export const ingressClass = style({
  fontSize: "1.33rem",
  margin: 0,
  marginBottom: "1.5em"
});
